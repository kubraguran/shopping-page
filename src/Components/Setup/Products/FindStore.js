import React from 'react';

const FindStore = () => {
    return (
        <div className='findstore' > 
            <h2 className='storeTitle'>STORE FINDER</h2>
            <p className='subTitle'>ENTER A LOCATION (FOR EXAMPLE: A POST CODE, AN ADDRESS, A CITY, ETC.) AND WE WILL SHOW YOU THE CLOSEST STORES.</p>
        </div>
    )
};

export default FindStore;
